apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}
spec:
  selector:
    app: {{ .Values.app.name }}
    color: {{ .Values.color }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http